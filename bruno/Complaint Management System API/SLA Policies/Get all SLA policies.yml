info:
  name: Get all SLA policies
  type: http
  seq: 1
  tags:
    - SLA Policies

http:
  method: GET
  url: "{{baseUrl}}/api/v1/sla-policies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/sla-policies"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
