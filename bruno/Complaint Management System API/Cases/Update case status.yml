info:
  name: Update case status
  type: http
  seq: 4
  tags:
    - Cases

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/cases/:id/status"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "status": "",
        "note": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Updated
    request:
      url: "{{baseUrl}}/api/v1/cases/:id/status"
      method: PATCH
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "status": "",
            "note": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
