info:
  name: Add case activity
  type: http
  seq: 3
  tags:
    - Cases

http:
  method: POST
  url: "{{baseUrl}}/api/v1/cases/:id/activities"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "activityType": "Note",
        "description": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Created
    request:
      url: "{{baseUrl}}/api/v1/cases/:id/activities"
      method: POST
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "activityType": "Note",
            "description": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
