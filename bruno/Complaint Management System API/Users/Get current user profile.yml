info:
  name: Get current user profile
  type: http
  seq: 6
  tags:
    - Users

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/users/me"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
