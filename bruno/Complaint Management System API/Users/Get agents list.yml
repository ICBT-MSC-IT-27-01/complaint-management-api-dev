info:
  name: Get agents list
  type: http
  seq: 7
  tags:
    - Users

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/agents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/users/agents"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
