info:
  name: Delete user
  type: http
  seq: 5
  tags:
    - Users

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/users/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Deleted
    request:
      url: "{{baseUrl}}/api/v1/users/:id"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
