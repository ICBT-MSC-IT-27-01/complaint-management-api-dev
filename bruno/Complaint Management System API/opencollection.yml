opencollection: 1.0.0

info:
  name: Complaint Management System API

request:
  auth:
    type: bearer
    token: "{{token}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
