info:
  name: Update client
  type: http
  seq: 4
  tags:
    - Clients

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/clients/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Updated
    request:
      url: "{{baseUrl}}/api/v1/clients/:id"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
