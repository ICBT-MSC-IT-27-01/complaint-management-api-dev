info:
  name: Client typeahead search
  type: http
  seq: 6
  tags:
    - Clients

http:
  method: GET
  url: "{{baseUrl}}/api/v1/clients/search"
  params:
    - name: q
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/clients/search"
      method: GET
      params:
        - name: q
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
