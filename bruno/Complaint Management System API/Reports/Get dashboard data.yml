info:
  name: Get dashboard data
  type: http
  seq: 1
  tags:
    - Reports

http:
  method: GET
  url: "{{baseUrl}}/api/v1/reports/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/reports/dashboard"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
