info:
  name: Get complaint attachments
  type: http
  seq: 1
  tags:
    - Attachments

http:
  method: GET
  url: "{{baseUrl}}/api/v1/complaints/:complaintId/attachments"
  params:
    - name: complaintId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/complaints/:complaintId/attachments"
      method: GET
      params:
        - name: complaintId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
