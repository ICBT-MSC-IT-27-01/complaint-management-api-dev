info:
  name: Deactivate category
  type: http
  seq: 5
  tags:
    - Categories

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/categories/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Deactivated
    request:
      url: "{{baseUrl}}/api/v1/categories/:id"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
