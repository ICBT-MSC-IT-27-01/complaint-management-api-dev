info:
  name: Get all categories
  type: http
  seq: 1
  tags:
    - Categories

http:
  method: GET
  url: "{{baseUrl}}/api/v1/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/categories"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
