info:
  name: Close complaint
  type: http
  seq: 10
  tags:
    - Complaints

http:
  method: POST
  url: "{{baseUrl}}/api/v1/complaints/:id/close"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Closed
    request:
      url: "{{baseUrl}}/api/v1/complaints/:id/close"
      method: POST
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
