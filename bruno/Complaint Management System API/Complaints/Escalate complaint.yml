info:
  name: Escalate complaint
  type: http
  seq: 8
  tags:
    - Complaints

http:
  method: POST
  url: "{{baseUrl}}/api/v1/complaints/:id/escalate"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "reason": "",
        "escalatedToUserId": 0,
        "escalationType": "Manual"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Escalated
    request:
      url: "{{baseUrl}}/api/v1/complaints/:id/escalate"
      method: POST
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "reason": "",
            "escalatedToUserId": 0,
            "escalationType": "Manual"
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
