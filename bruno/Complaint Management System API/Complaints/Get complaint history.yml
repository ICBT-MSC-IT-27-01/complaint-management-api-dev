info:
  name: Get complaint history
  type: http
  seq: 5
  tags:
    - Complaints

http:
  method: GET
  url: "{{baseUrl}}/api/v1/complaints/:id/history"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/complaints/:id/history"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
